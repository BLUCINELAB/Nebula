# Nebula Companion

Applicazione web offline minimalista. Fornisce una domanda del giorno, un campo di testo salvato in `localStorage` e una frase poetica di ispirazione.

## Uso
Aprire `index.html` in un browser moderno. Nessuna connessione esterna è necessaria.
